// Minimal PDF generator stub. Replace with real implementation (pdfkit, puppeteer, etc.)
function generatePdf(bufferOrHtml) {
  // return a Buffer placeholder
  return Buffer.from('PDF-PLACEHOLDER');
}

module.exports = { generatePdf };
